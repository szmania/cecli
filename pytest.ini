[pytest]
norecursedirs = tmp.* build benchmark _site OLD
addopts = -p no:warnings
asyncio_mode = auto
testpaths =
    tests/basic
    tests/tools
    tests/help
    tests/browser
    tests/scrape
    tests/coders

env =
    AIDER_ANALYTICS=false

