# Plan: Implement RunProcess Tool and Create Comprehensive Tool Tests

This plan details the steps to replace the `RunAider` tool with a more generic `RunProcess` tool, and then create a thorough test script to validate all tool-management commands.

## 1. Implement `RunProcess` Tool
- [X] Create a new tool file `aider/tools/run_process.py` based on the old `aider/tools/run_aider.py`.
- [X] Modify the new tool to be a generic command runner, accepting a `command` argument.
- [X] Delete the old `aider/tools/run_aider.py` file.
- [X] Register the new `run_process` tool in `aider/coders/agent_coder.py` by adding it to the imports and `tool_modules` list.

## 2. Create Testable Slash Command
- [X] Add a new `/runprocess` slash command in `aider/commands.py`. This command will act as a wrapper around the `RunProcess` tool to make it easily testable from the command line.

## 3. Update Documentation
- [X] Update the configuration example in `aider/website/docs/config/agent-mode.md` to include `runprocess` in the `tools_excludelist`.

## 4. Create Tool Test Script
- [X] Create a comprehensive test script in `.aider.test.commands.txt`.
- [X] This script will be used with `aider --message "/load .aider.test.commands.txt"` to perform an end-to-end test of all tool management commands.
- [X] The script will test:
    - `/tools` (listing)
    - `/tools-create` (local and global scope, with and without filename)
    - `/tools-unload` (custom and standard tools, by name and path)
    - `/tools-load` (custom and standard tools, by name and path)
    - `/tools-move` (changing scope between local and global)
    - `/runprocess` (the new testable command)
    - `/tools-edit` (adding a tool file to the chat for editing)

## 5. Final Review
- [X] Review all changes to ensure they are correct and complete.
